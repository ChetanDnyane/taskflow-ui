# The Vite server proxies /api to Spring Boot. This value is server-side only.
API_PROXY_TARGET=http://127.0.0.1:8080
# Keep /api for same-origin production hosting. Never put JWT_SECRET here.
VITE_API_BASE_URL=/api
